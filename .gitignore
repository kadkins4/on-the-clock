node_modules
dist/
.DS_Store
*.bak
.vercel
.claude/worktrees/
# Local Claude Code harness config — per-machine (e.g. worktree bgIsolation);
# must stay local or it changes the workflow on other machines.
.claude/settings.json

# This project uses npm (package-lock.json). Keep stray pnpm lockfiles out.
pnpm-lock.yaml

# Playwright MCP scratch output
.playwright-mcp/

# Brainstorming visual-companion mockups (local scratch)
.superpowers/

# Local env overrides — never commit secrets here. The tracked .env holds only
# the public Formspree endpoint; per-machine/secret values go in .env*.local.
.env.local
.env.*.local
