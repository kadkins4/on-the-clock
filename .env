# Alpha suggestion box → Formspree. Public endpoint (embedded client-side, not a
# secret); committed so local dev and the Vercel build both pick it up. Override
# per-machine in .env.local if needed. See .env.local.example.
VITE_FORMSPREE_ENDPOINT=https://formspree.io/f/mgobwkjw
